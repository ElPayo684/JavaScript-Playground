// Array, solo tiene valores

const numbers = [10, 20, 30];

// Objeto, llave y valor

const producto = {
    name: 'Monitor de 20 pulgadas',
    price: 200,
    available: true
}

// Array pero con constructor

const month = new Array('January', 'February', 'March');

// El Array puede contener de todo tipo de valores almacenados

const anything = ['Pedro', 20, 'like', true, null, { name: 'Pedro', age: 13 }, [1, 2, 3]];

console.log(numbers);
// console.log(producto);
console.log(month);
console.log(anything);